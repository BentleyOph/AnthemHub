# SQL Assets

Place Supabase DDL, RLS policies, views, and seed data in this directory. 
Each logical change set should live in its own `.sql` file so it can be applied via Supabase migrations tooling.
